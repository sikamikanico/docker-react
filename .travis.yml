sudo: required
services:
  - docker

before_install:
  - docker build -t fedemoreira/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true fedemoreira/docker-react npm run test

language: generic
